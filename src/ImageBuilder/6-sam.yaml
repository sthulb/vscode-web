name: SAM CLI
description: Install SAM CLI
schemaVersion: 1.0

phases:
  - name: build
    steps:
      - name: Install
        action: ExecuteBash
        inputs:
          commands:
            - pip3 install aws-sam-cli